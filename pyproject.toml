[project]
name = "nodered-backup"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.3",
    "django-apscheduler>=0.7.0",
    "gunicorn>=25.3.0",
    "whitenoise>=6.12.0",
    "docker>=7.0.0",
    "watchdog>=4.0.0",
]
