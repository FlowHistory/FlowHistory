services:
  flowhistory:
    build: .
    ports:
      - "9473:8000"
    environment:
      - DEBUG=true
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-localhost,127.0.0.1}
      - TZ=${TZ:-UTC}
      - REQUIRE_AUTH=false
    restart: unless-stopped
